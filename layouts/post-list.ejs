<% if (typeof tag != "undefined") { %>
  <h1 class="mt-4">
    Tag: <%= tag %>
  </h1>
<% } %>

<div class="my-3">
  <% for(var i = 0; i < posts.length; i++) { %>
    <% if (typeof posts[i].teaser != "undefined") { %>
      <%- include('./post-list-element', { post: posts[i] }); %>
    <% } else { %>
      <%- include('./post', { post: posts[i] }); %>
    <% } %>
  <% } %>
</div>
